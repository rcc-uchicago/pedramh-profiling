# SPDX-FileCopyrightText: Copyright (c) 2023 - 2026 NVIDIA CORPORATION & AFFILIATES.
# SPDX-FileCopyrightText: All rights reserved.
# SPDX-License-Identifier: Apache-2.0
#
# SfnoPlasim single-stage training config — matches PanguWeather v2.0
# SFNO_PLASIM_H5_DERECHO_5412.yaml scheduler block.

use_static_capture: False             # opt-in StaticCaptureTraining + CUDA graphs (PhysicsNeMo decorator)
amp: none
grad_clip_norm: 0.0
max_epochs: 50

# DDP / compile tuning (opt-in; defaults preserve prior behavior). See
# train.py's DDP-wrap comment for the makani / PanguWeather-e3sm provenance.
ddp_bucket_cap_mb: null               # null = PyTorch's 25MB default bucket size
ddp_static_graph: False               # safe to enable here (checkpointing branches on a fixed hyperparameter, not data-dependent control flow)
jit_compile: False                    # wraps the DDP model with torch.compile(); unvalidated for the vendored SHT/complex-number ops

optimizer:
  type: AdamW
  lr: 1.0e-4
  weight_decay: 3.0e-6
  fused: null

ema:
  enabled: False
  decay: 0.999
  warmup_epochs: 6
  # Validate with the EMA weights (matches inference use_ema=true). Explicit for
  # the SFNO ERA5 runs; False validates the raw training weights instead.
  validate_with_ema: True

stages:
  - name: default
    num_epochs: ${training.max_epochs}
    unroll_steps: 1
    max_iterations: .inf
    batch_size: null
    scheduler:
      type: LinearWarmupCosineAnnealingLR
      num_warmup_epochs: 5
      warmup_start_lr: 1.0e-8
      eta_min: 1.0e-8
